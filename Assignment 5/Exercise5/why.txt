The variables inside the struct will be stored adjacently so there is a possibility of overflow. Transfer_money() will be called if we can fix the code so that check_pin and local_secre_pin are actually equal.